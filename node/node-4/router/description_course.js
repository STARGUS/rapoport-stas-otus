const router = require("express").Router();

router.get("/findAll", (req, res) => {
  res.send({ data: [] });
});
router.get("/findAll", (req, res) => {
  res.send({ data: [] });
});

module.exports = router;
